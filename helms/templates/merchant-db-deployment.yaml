apiVersion: apps/v1
kind: Deployment
metadata:
  name: merchant-db-deployment
spec:
  replicas: {{ .Values.merchantDb.replicaCount }}
  selector:
    matchLabels:
      component: merchant-db
  template:
    metadata:
      labels:
        component: merchant-db
    spec:
      containers:
      - name: merchant-db
        image: "{{ .Values.merchantDb.image.repository }}:{{ .Values.merchantDb.image.tag }}"
        args: ["--default-authentication-plugin=mysql_native_password"]
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.merchantDb.secret.name }}
              key: mysql-root-password
        ports:
        - containerPort: {{ .Values.merchantDb.service.port }}
        volumeMounts:
        - name: merchant-db-vol
          mountPath: /var/lib/mysql
        - name: init-script
          mountPath: /docker-entrypoint-initdb.d
      volumes:
      - name: merchant-db-vol
        persistentVolumeClaim:
          claimName: {{ .Values.merchantDb.pvc.name }}
      - name: init-script
        configMap:
          name: {{ .Values.merchantDb.initScript.name }}

